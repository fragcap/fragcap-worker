name = "fragcap-worker"
main = "src/index.js"
compatibility_date = "2024-12-01"

[[kv_namespaces]]
binding = "RATE_LIMIT"
id = "699f600571a049d49a6f091f93cd18cb"

# Secrets (do not set here — use wrangler secret put):
# - APP_ID          GitHub App ID
# - PRIVATE_KEY     GitHub App private key (.pem contents)
# - INSTALLATION_ID GitHub App installation ID after installing on the registry repo
